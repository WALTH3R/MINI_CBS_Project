LOAD DATABASE
     FROM sqlite:///data/db.sqlite3
     INTO postgresql://minicbs:minicbs_dev_password@db:5432/mini_cbs

WITH include no drop, create no tables, create no indexes,
     reset no sequences, data only

SET work_mem to '128MB', maintenance_work_mem to '512MB';
